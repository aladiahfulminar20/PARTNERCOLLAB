#include <stdio.h>

/*
void swapString(char *a, char *b)
{

    char temp;

    temp = *a;
    *a = *b;
    *b = temp;
}

int main()
{
    char *name1 = "Larry", *name2 = "Albert";

    printf("Before swapping:");
    printf("\nName 1: %s", name1);
    printf("\nName 2: %s", name2);

    swapString(&name1, &name2);

    printf("\nAfter swapping:");
    printf("\nName 1: %s", name1);
    printf("\nName 2: %s", name2);
}
*/

int main()
{

    /*
    int myAge = 13;
    int *ptr = &myAge;

    printf("\n%p", ptr);
    printf("\n%d", *ptr);
    */

    /*
    int *age_pointer;
    int age = 20;

    char *name_pointer;
    char *name = "Reiu";

    age_pointer = &age;
    name_pointer = name;

    printf("\n%d", *age_pointer);
    printf("\n%s", name_pointer);
    */
}